# 黑马记账 · 项目总文档

> 本文件是项目的总说明书，包含「产品文档」和「协作规则」两部分。
> 每次开发前先阅读本文件，尤其是协作规则。

---

## 一、产品文档

### 1. 项目概况

| 项目 | 内容 |
| --- | --- |
| 应用名称 | 黑马记账 |
| 应用类型 | 个人桌面记账软件（单机使用，数据保存在本地） |
| 目标平台 | Windows 10/11、macOS |
| 开发语言 | C++ + Qt 6（图形界面框架） |
| 界面语言 | 简体中文 |
| 目标用户 | 零基础的个人用户，用于日常记录人民币花销 |

### 2. 核心功能（第一期必须完成）

1. **记一笔账**：输入金额（人民币，单位元，支持两位小数）、选择分类（先一级大类，再二级小类）、选择日期（默认当天，可修改）、可填写备注（选填）。
2. **两级分类**：分类体系见下方表格，由 Claude 设计，用户可随时要求增删改。
3. **查看账单**：按时间顺序查看历史账单；支持按日期、按分类筛选。
4. **数据保存**：数据保存在本地，关闭软件后不丢失。

### 3. 后续可扩展功能（非第一期；是否做、何时做由用户决定）

- 月度 / 年度统计报表与图表（如各分类支出占比饼图）
- 收入记账、预算管理
- 数据导出（Excel / CSV）与备份
- 应用图标与主题配色

### 4. 花销分类体系（一级大类 → 二级小类）

| 一级大类 | 二级小类 |
| --- | --- |
| 餐饮饮食 | 早餐、午餐、晚餐、夜宵、外卖、零食饮料、水果、聚餐请客 |
| 交通出行 | 公交地铁、打车网约车、加油充电、停车费、火车机票、汽车保养维修 |
| 购物消费 | 日用品、服饰鞋包、电子产品、美妆护肤、家居厨具 |
| 居住日常 | 房租房贷、水费、电费、燃气费、物业费、维修维护 |
| 娱乐休闲 | 电影演出、游戏、旅行出游、运动健身、宠物 |
| 医疗健康 | 看病买药、体检保健、住院医疗 |
| 学习教育 | 书籍文具、课程培训、学费 |
| 人情往来 | 红包礼金、送礼、孝敬长辈 |
| 通讯网络 | 手机话费、宽带网费、软件会员 |
| 其他杂项 | 其他（无法归入以上大类的花销） |

### 5. 界面初步构想（文字版）

- 主窗口分三个区域：
  - 顶部工具栏：「记一笔」按钮、日期筛选、分类筛选；
  - 中部账单列表：按时间倒序显示每笔账（日期、分类、备注、金额）；
  - 底部汇总栏：显示当前筛选范围内的总支出。
- 「记一笔」弹窗：金额 → 两级分类下拉 → 日期 → 备注 → 保存。
- 金额用人民币格式显示（如 ¥ 25.50）。

### 6. 待定决策清单（待用户拍板，由 Claude 负责用中文解释方案）

- [ ] 应用图标、配色风格（开发后期再定）

---

## 二、协作规则（重要，全程严格遵守）

### 1. 用户背景

用户是**零编程基础**的初学者，首次使用 Claude，无法给出技术性具体要求，也不应被要求阅读或理解代码。

### 2. 技术决策流程（强制）

每当出现技术选型决策点（框架、存储方式、工具等）时：

1. Claude 用**简体中文、通俗语言**列出 2~4 个可行方案；
2. 每个方案必须说明**优势、劣势**和**适用场景**；
3. Claude 可以给出自己的推荐及理由，但**最终决定权在用户**；
4. 用户选定后，把决定记入本文档「决策记录」，后续不再重复询问；
5. 禁止替用户做技术选型决定，禁止未解释就默认采用某个方案。

### 3. 沟通方式

- 与用户的所有沟通使用**简体中文**；
- 解释技术概念时使用**生活化类比**，避免专业术语；必须使用术语时先解释；
- 每次只聚焦一个决策点，不把多个问题一次性抛给用户；
- 用简短进度汇报（如「已完成 xx，接下来做 yy」），不要把大段代码或日志直接丢给用户；
- 重要变更（功能调整、数据格式变化、界面改动）先征得用户同意再动手。

### 4. 开发方式

- 代码注释、提交说明使用中文；
- 按小步迭代开发：完成一个小功能 → 告诉用户如何试运行 → 用户确认后再继续；
- 涉及已有数据格式变更时，先说明对旧数据的影响并征得同意；
- 安装打包方式（安装包 / 绿色免安装版）在开发后期向用户解释并由用户选择。

---

## 三、决策记录

| 日期 | 决策点 | 用户的选择 | 备注 |
| --- | --- | --- | --- |
| 2026-09-14 | 图形界面框架 | **Qt 6** | 四个方案（Qt 6 / wxWidgets / Dear ImGui / FLTK）对比后由用户选定；理由：中文支持好、界面美观、自带数据库与图表、资料丰富 |
| 2026-09-14 | 开发环境安装方式 | **复用 VS 编译器 + 自动下载 Qt 6** | 用户电脑已有 VS（D:\vs2，MSVC 14.44/14.51）和 Dev-Cpp(g++)；Qt 库用命令行工具自动下载，无需注册账号、零手动操作 |
| 2026-09-14 | 数据存储方式 | **SQLite 数据库** | 三个方案（SQLite / JSON 文本 / CSV 表格）对比后由用户选定；Qt 自带 QSQLITE 驱动无需额外安装；数据库文件位于系统应用数据目录（C:\Users\<用户名>\AppData\Roaming\黑马\黑马记账\heima_accounting.db） |

---

## 四、开发环境说明（机器相关，供 Claude 参考）

| 项目 | 位置 / 版本 |
| --- | --- |
| 操作系统 | Windows 11（当前开发机） |
| C++ 编译器 | MSVC（Visual Studio，安装于 D:\vs2，工具集 14.44 / 14.51） |
| Qt 6 | D:\Qt\6.8.3\msvc2022_64 |
| CMake / Ninja | VS 自带：D:\vs2\Common7\IDE\CommonExtensions\Microsoft\CMake\（CMake\bin 与 Ninja） |
| Python | D:\python（3.14，已装 aqtinstall，用于自动下载 Qt） |
| 编译方式 | 双击 tools\build.bat（或命令行：先调用 D:\vs2\VC\Auxiliary\Build\vcvars64.bat，再 cmake -G Ninja 编译） |
| 运行方式 | 双击 tools\run.bat |

备注：Mac 版编译需要在 macOS 上进行（代码本身跨平台，无需改动），后期再安排。

### 5. 命令行编译注意事项（血泪经验，务必遵守）

- 在 Git Bash 中调用 `cmd /c` 时，命令里的**引号会被 MSYS2 转换破坏**、`>nul` 会把 `nul` 当路径转换导致报错。解决：设 `MSYS2_ARG_CONV_EXCL='*'`，且命令中**不要出现引号和 >nul**（路径恰好都不含空格）。
- `cmd /c` 的长链条中 `%VAR%` 在整行解析时就展开（会拿到 vcvars 设置前的旧值），必须用 `cmd /v:on /c` + `!VAR!` 延迟展开。
- 调用 vcvars64.bat 前必须先 `set PATH=D:\;%PATH%`（vswhere.exe 已复制到 D:\vswhere.exe），否则 VS 定位失败、cl 不在 PATH。
- 已验证可用的编译命令模板：

```
cd "/d/黑马记账 app" && MSYS2_ARG_CONV_EXCL='*' cmd /v:on /c 'set PATH=D:\;%PATH% && call D:\vs2\VC\Auxiliary\Build\vcvars64.bat && set PATH=D:\vs2\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin;D:\vs2\Common7\IDE\CommonExtensions\Microsoft\CMake\Ninja;!PATH! && cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH=D:/Qt/6.8.3/msvc2022_64 && cmake --build build'
```

- 用户手动编译请双击 tools\build.bat（原生 cmd 环境无上述问题）。
- 运行程序：PATH 需包含 D:\Qt\6.8.3\msvc2022_64\bin（Qt 运行库），或双击 tools\run.bat。
